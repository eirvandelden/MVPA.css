# Local lefthook configuration for mvpa.css gem
# Extends the global lefthook.yml from dotfiles

pre-commit:
  commands:
    rubocop:
      skip: true
    eslint:
      skip: true
    csslint:
      skip: true

pre-push:
  skip:
    - bundle-audit
    - brakeman
    - minitest
    - rspec
